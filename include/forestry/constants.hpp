#pragma once

#include <cstdint>
#include <string>

namespace forestry {
  inline static constexpr std::int32_t chopping_anim_id = 875;

  inline static std::vector<std::string> log_names = {
    "Logs",
    "Oak logs",
    "Willow logs"
  };
}